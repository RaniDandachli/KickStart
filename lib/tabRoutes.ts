import type { Href } from 'expo-router';

/** Programmatic tab links · widened when `.expo/types/router.d.ts` hasn’t been regenerated yet. */
export function moneyChallengesHref(): Href {
  return '/(app)/(tabs)/money-challenges' as unknown as Href;
}
