import { Text } from 'react-native';

import { Screen } from '@/components/ui/Screen';

/** Placeholder — prize pool / rewards catalog UI goes here. */
export default function PrizesScreen() {
  return (
    <Screen>
      <Text className="mb-2 text-3xl font-black text-white">Prizes</Text>
      <Text className="text-base font-medium text-slate-300">Coming soon — rewards and prizes will show here.</Text>
    </Screen>
  );
}
