/**
 * Procedural content for Neon Grid (Crossy-style lanes).
 * Rows are currently generated in `engine.ts` via `mulberry32` + `nextRow`.
 *
 * TODO (Claude pass): moving hazards, river/logs, trains, biome themes, H2H shared seed chunks.
 */

export {};
